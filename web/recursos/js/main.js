// Función para abrir el popup
function openPopup(popNumber) {
    document.getElementById(popNumber).style.display = 'flex';
}

// Función para cerrar el popup
function closePopup(popNumber) {
    document.getElementById(popNumber).style.display = 'none';
}
